import numpy as np
from statistics import mode

pencils = [9,25,23,12,11,6,7,8,9,10]

mean_val = np.mean(pencils)

median_val = np.median(pencils)

mode_val = mode(pencils)

print("Mean:", mean_val)
print("Median:", median_val)
print("Mode:", mode_val)
